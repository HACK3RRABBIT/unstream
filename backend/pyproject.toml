[project]
name = "unstream-api"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    # Installs as a yt-dlp plugin: it needs no import, and does nothing until
    # POT_PROVIDER_URL names a provider for it to ask. See app/ytdlp.py.
    "bgutil-ytdlp-pot-provider>=1.3.1",
    "fastapi>=0.139.0",
    "httpx>=0.28",
    "libtorrent>=2.0.11; python_version < '3.14'",  # torrent engine for Nyaa
    "mutagen>=1.48.1",
    "pycryptodome>=3.20",
    "uvicorn[standard]>=0.51.0",
    "yt-dlp>=2026.7.4",
]

[dependency-groups]
dev = [
    "httpx2>=2.5.0",
    "pytest>=8.4.2",
]
